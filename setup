#!/usr/bin/env bash
set -euo pipefail

SKILL_DIR="$(cd "$(dirname "$0")" && pwd)"
cd "$SKILL_DIR"

if [ ! -d ".venv" ]; then
    echo "Creating virtual environment (Python 3.11+)..."
    uv venv --python 3.11
fi

echo "Installing dependencies..."
uv pip install -e .

echo "Setup complete."
