#!/usr/bin/env bash
set -euo pipefail

SKILL_DIR="$(cd "$(dirname "$0")" && pwd)"
cd "$SKILL_DIR"

echo "Creating the locked environment and installing dependencies..."
uv sync --locked

echo "Setup complete."
